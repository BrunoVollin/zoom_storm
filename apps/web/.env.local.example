# Server-only: base URL of the BFF. Never exposed to the browser (no NEXT_PUBLIC_ prefix)
# because the route handlers are the only thing allowed to talk to it.
BFF_BASE_URL=http://localhost:8088

# Public: canonical URL of this storefront, used for absolute links/metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3100
